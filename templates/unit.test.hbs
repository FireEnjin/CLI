// import { connectFirestore } from "@fireenjin/graphql";
import {{nameFromPathCamel name}} from "./{{nameFromPathCamel name}}";

describe("{{titleCase name}}", () => {
  // beforeAll(() => {
  //   connectFirestore({
  //     serviceAccount: true,
  //   });
  // });
  it("Should {{description}}", async () => {
    const response = await {{nameFromPathCamel name}}({});
    console.log(response);
    expect(response).toMatchObject({});
  });
});